# Cleaning decisions log

Generated by clean.py. Every count below is computed live, not hardcoded.

## Zip code leading zeros
- Reloaded customer/seller/geolocation zip prefixes as zero-padded text.
- Without this fix, 23995 customer rows, 1027 seller rows, and 245733 geolocation rows would have a truncated zip prefix.

## Orders
- Flagged 8 rows: status 'delivered' with no delivery date. Excluded from delivery-timing analysis via valid_for_delay_calc, kept for revenue.
- Flagged 1359 rows: carrier pickup logged before approval. Excluded from delay calc.
- Flagged 23 rows: customer received before carrier pickup. Excluded from delay calc.
- 98051 of 99441 orders are valid for delay calculation.
- delivery_status and days_late are only meaningful where valid_for_delay_calc is True.

## Order items
- No nulls, no duplicate rows, no non-positive prices found.
- 383 rows have freight_value of 0. Kept as-is, not treated as an error.
- 112650 item rows cover only 98666 distinct orders. Built order_items_agg.csv at order grain for any join to orders; the item-level file is kept separately for the freight-by-category analysis, which needs item grain.

## Payments
- Flagged 9 rows with payment_value of 0 (mostly vouchers). Kept, flagged, excluded from revenue sums by nature of being 0.
- Flagged 3 rows with payment_type 'not_defined'. Excluded from payment-type breakdowns.
- Built payments_agg.csv at order grain, same reasoning as order_items_agg.

## Reviews
- 551 orders had more than one review row. Kept the review with the latest review_answer_timestamp per order in reviews_dedup.csv; this is the file to join to orders, the raw reviews file is kept separately and should not be joined 1:1 to orders.
- 789 review_id values are reused across different order_ids. Always join on order_id, never review_id.
- Comment title and message nulls are sparse text, not treated as an error, left as null.

## Products
- Patched 2 category names missing from the translation table: ['pc_gamer', 'portateis_cozinha_e_preparadores_de_alimentos'].
- Flagged 610 rows with no category at all (also missing all other descriptive fields). Category set to 'unknown', rows kept, not dropped.
- 2 rows still missing weight and dimensions after the above. Left as null, flagged for exclusion from any weight-dependent calculation.

## Geolocation
- Dropped 261831 exact duplicate rows.
- Dropped 27 rows with lat/lng outside Brazil's bounding box.
- Collapsed to one row per zip prefix (mean lat/lng, most common city and state spelling): 19011 rows, down from 1000163. This is the file to join for regional lookups; the raw file has too many inconsistent city-name spellings to group on directly.

## Customers
- No nulls, no duplicate customer_id.
- 99441 distinct customer_id but only 96096 distinct customer_unique_id. Any repeat-purchase or retention metric must group on customer_unique_id, not customer_id.

## Sellers
- Clean: no nulls, no duplicate seller_id.